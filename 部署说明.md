# 合同审查助手 · 部署说明（换机器 / 交给别人跑）

> 这是个**本地跑**的 Web 应用：FastAPI + 本地向量库（Milvus Lite）+ 本地嵌入模型（bge-m3）+ 本地 OCR（MinerU）。
> **只有"跟大模型对话"这一步需要联网**，且需要一把**自己的** DeepSeek API Key。

---

## 0. 换机器要准备三样东西

1. **Python 3.11 环境**（开发机为 3.11.16）
2. **两个模型目录**：bge-m3（2.2 GB）、MinerU 模型（1.1 GB）
3. **一把自己的 DeepSeek API Key**（不要用别人的，见 §2.4）

---

## 1. 运行环境要求

| 项 | 要求 |
|---|---|
| 操作系统 | Windows 10/11（开发机环境；Linux/macOS 也能跑，MinerU 路径按平台自动切换） |
| Python | **3.11** |
| 磁盘 | 源码+数据 ~5 MB；bge-m3 2.2 GB；MinerU 模型 1.1 GB；依赖装完约 4~6 GB |
| 内存 | 建议 ≥ 8 GB（bge-m3 + MinerU 同时加载） |
| 网络 | 仅"对话"需要（DeepSeek API）。解析、检索、向量化全部本地 |

---

## 2. 首次安装（约 15~30 分钟，看网速）

### 2.1 建虚拟环境
```bat
cd /d <项目目录>
python -m venv .venv
.venv\Scripts\python.exe -m pip install --upgrade pip
```

### 2.2 装依赖
```bat
.venv\Scripts\python.exe -m pip install -r requirements.txt
```
（**完全离线**的情况见 §3）

### 2.3 放模型（两个）

| 模型 | 放到哪里 | 大小 | 干什么用的 |
|---|---|---|---|
| **bge-m3** | `项目\models\bge-m3\`（整个目录拷过来） | 2.2 GB | 合同条款 / 法条向量化（本地） |
| **MinerU 模型** | `项目\models\`（就在包里，核对一下） | 1.1 GB | PDF / 扫描件解析 |

- bge-m3 也可以放到别处，用环境变量指过去：在 `.env` 里写 `BGE_MODEL_PATH=D:\models\bge-m3`
- 自检：`项目\models\bge-m3\pytorch_model.bin` 这个文件应该存在

### 2.4 配自己的 API Key
```bat
copy .env.example .env
notepad .env
```
把 `DEEPSEEK_API_KEY=sk-在这里填你自己的key` 改成自己的。申请地址：https://platform.deepseek.com/ → API keys → 创建。

> ⚠️ `.env` 是私钥文件：**不要提交到 git，也不要打包给别人**。给别人就给他 `.env.example`。

### 2.5 启动
```bat
.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```
看到这几行才算就绪（bge-m3 预热要 30~90 秒，期间控制台不动是正常的）：
```
[startup] bge-m3 已就绪
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8000
```
浏览器打开 **http://127.0.0.1:8000/**

---

## 3. 三种装依赖的方式

| 方式 | 做法 | 适用 |
|---|---|---|
| **A. 联网装（推荐）** | 见 §2.2 | 目标机能上网，下载约 5 GB（含 torch） |
| **B. 离线 wheel 包** | 在**有网**的机器上跑 `pip download -r requirements.txt -d wheels`，把 `wheels\` 一起拷过去；目标机 `pip install --no-index --find-links=wheels -r requirements.txt` | 目标机不能上网 |
| **C. 直接拷 `.venv`** | ❌ **不推荐** | venv 不可移植：`.venv\pyvenv.cfg` 里写死了原机器的解释器路径（`C:\Users\15398\AppData\Roaming\uv\python\...`）。除非目标机把 Python 3.11 装到完全相同的路径，否则必然失败 |

> 注意区分：**模型**可以不联网（打包带走），但 **Python 依赖**仍然要装。想彻底离线就用方式 B。

---

## 4. 打包清单

**要带：**
- `app\`、`scripts\`、`make_test_pdf.py`（源码）
- `data\`（合同、审查意见、Milvus 向量库、MinerU 解析产物；约 1.1 MB）
- `models\`（MinerU 模型 1.1 GB）
- `models\bge-m3\`（建议放进去，让包自包含；不放就得单独拷 2.2 GB 并配 `BGE_MODEL_PATH`）
- `requirements.txt`、`.env.example`、`部署说明.md`

**不要带：**
- `.venv\`（不可移植，且几个 GB）
- `.env`（**你的私钥**）
- `data\*.bak*`（调试时的数据库备份）
- `data\~$*.docx`（Word 打开文档时产生的临时文件）

---

## 5. 常见问题

| 现象 | 原因 / 处理 |
|---|---|
| 对话框回「未配置 DEEPSEEK_API_KEY」 | `.env` 没建或没填 → §2.4 |
| 启动日志 `⚠ bge-m3 模型目录不存在` | 模型没放好 → §2.3 |
| `[Errno 10048] error while attempting to bind on address` | 8000 被占：`netstat -ano \| findstr :8000` 找到 PID，`taskkill /PID <pid> /F`；或换端口 `--port 8001` |
| `DataDirLockedError: another process holds the lock on ...\milvus.db` | 已经有一个实例在跑。Milvus Lite 是嵌入式数据库，**同一数据目录同一时刻只允许一个进程**，先关掉那个 |
| 第一次解析合同要几十秒 | MinerU 首次加载模型，正常 |
| 检索（标准条款库/法条库）一直返回空 | 向量库没建：跑一次 `.venv\Scripts\python.exe -m app.init_db`（⚠️ 会**清空并重建**条款库/法条库与两个 Milvus 集合） |
| 想确认用的哪把 key | 启动日志不会打印 key；用 `curl` 查余额即可（不要把 key 打到聊天/截图里） |

---

## 6. 想清空成"全新"状态

- **只清合同与审查意见**：删掉 `data\contract.db` 再重启（会自动建空表）。注意条款库/法条库存在 Milvus 里，**不受影响**。
- **连条款库/法条库一起重置**：`.venv\Scripts\python.exe -m app.init_db`（`reset=True`，会重建两个集合，需要 bge-m3 在位）
